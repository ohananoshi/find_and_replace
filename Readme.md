### It reproduces the notepad's functionality of replacing all occurrences in addition to being able to receive more keywords at once.

```Python
    find_and_replace(input_string, words_dictionary)

    returns: converted string
```